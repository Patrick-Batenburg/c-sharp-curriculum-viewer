@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
	ViewData["Title"] = "Index";
}

<div class="bg-primary m-1 p-1"><h4>Roles</h4></div>
<a class='pink-text' asp-action="Register" asp-controller="Account"><b>Nieuwe gebruiker</b></a>
<div class="text-danger" asp-validation-summary="ModelOnly"></div>
	@if (Model.Count() == 0)
	{
		<tr><td colspan="4" class="text-center">No Roles</td></tr>
	}
	else
	{
		<ul>
		@foreach (var role in Model)
		{
			<li>
				@role.Name
				<form style="display: inline;" asp-action="Delete" asp-route-id="@role.Id" method="post"><a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@role.Id">Edit</a></form>
			</li>
		}
		</ul style="display:>
	}